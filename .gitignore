# Build output
build/
build-*/
dist/
*.wasm
*.o
*.a

# Emscripten
.emscripten_cache/
em-cache/

# Game assets — never commit copyrighted data
*.big
assets/
game-data/

# Session-local state (codex session ids etc.)
.context/

# Tooling
node_modules/
.DS_Store
.cache/
compile_commands.json
.gstack/

# Nested repos with their own git history:
# - zh-web: GPL fork of GeneralsGameCode (Igroteka wasm branch)
# - dvijoke: the Dvijoke engine repo (github.com/meerzulee/dvijoke)
zh-web/
dvijoke/
