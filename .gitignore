# Build output
build/
build-*/
dist/
*.wasm
*.o
*.a

# Emscripten
.emscripten_cache/
em-cache/

# Game assets — never commit copyrighted data
*.big
assets/
game-data/

# Session-local state (codex session ids etc.)
.context/

# Tooling
node_modules/
.DS_Store
.cache/
compile_commands.json
.gstack/

# Site: engine builds are deploy artifacts (GPL wasm — publish only once the
# engine fork source is public); gamedata is a local dev symlink.
# Engine SOURCE now lives in sibling repos: zhelezo, fortochka, zh-web, dvijoke.
site/play/zh/GeneralsXZH.js
site/play/zh/GeneralsXZH.wasm
site/gamedata

# Cafe: local secrets for wrangler dev
.dev.vars

# Wrangler local state (D1 cache, dev server tmp) — root and cafe/
.wrangler/

# RTW play page: engine + workers are build-staged from fortochka/runtime/web
# (source there) + the wasm build. Only index.html is hand-authored/tracked.
site/play/rtw/worker.js
site/play/rtw/fsworker.js
site/play/rtw/fsproto.js
site/play/rtw/import.html
site/play/rtw/zhweb.js
site/play/rtw/rtw.menu.snap.gz
